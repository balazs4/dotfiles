# vim: set ft=tmux tw=0 nowrap:
set -g default-terminal "xterm-256color"
set-option -ga terminal-overrides ",xterm-256color:Tc"
set -g base-index 1
setw -g pane-base-index 1

set-option -g status-position bottom
set -g status-style "bg=colour235 fg=colour81"
set -g pane-border-style "fg=black"
set -g pane-active-border-style "bg=black fg=colour81"
set-option -g status-left "#S » "
set-option -g status-left-length 32
set-option -g status-right " « #(docker ps -a | sed 1,1d | wc -l) « %H:%M"
  
  # colour24
  # colour33
  # colour31
  # colour39
  # colour81
  # colour75
  # colour245
  # colour250
  # colour245
  # colour100
  # colour190
  # colour107
  # colour125
  # colour127
  # colour129
  # colour130
  # colour166
  # colour130
  # colour90
  # colour129
  # colour141
  # colour227
  # colour227
  # colour229
  # colour88
  # colour160
  # colour124
  # black
  # colour233
  # colour235
  # colour238
  # colour240
  # colour243
  # colour245

set -sg escape-time 0
set -g mouse on
set -g focus-events on

setw -g mode-keys vi
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R
bind-key -T copy-mode-vi v send-keys -X begin-selection
bind-key -T copy-mode-vi y send-keys -X copy-selection-and-cancel

bind-key u run-shell -b 'tmux capture-pane -p | xurls | fzf-tmux -w 96 -h 16 | xargs xdg-open > /dev/null || true'
bind-key m command-prompt -p "move pane to:"  "join-pane -t '%%' -h"

