#! /usr/bin/env bash

aerospace worksapce 1
aerospace list-windows --focused \
  | grep 'Alacritty' \
  | head -1 \
  | awk -F\| '{print $1}' \
  | xargs aerospace focus --window-id
aerospace list-windows --all | fzf --bind 'enter:execute(bash -c "aerospace focus --window-id {1}")+abort'
